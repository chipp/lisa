[package]
name = "transport"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
str_derive = { path = "../str_derive" }
uuid = { version = "1.6.1", features = ["v4", "fast-rng", "serde"] }

[dev-dependencies]
serde_json = "1.0.108"
